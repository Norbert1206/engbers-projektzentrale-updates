name: Build Engbers Projektzentrale 1.8.36b
on:
  push:
    paths:
      - 'build/1836/BUILD2'
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Patch stable 1.8.35
        run: |
          set -euo pipefail
          rm -rf /tmp/pz1836b
          mkdir -p /tmp/pz1836b/unpacked
          curl -fL 'https://raw.githubusercontent.com/Norbert1206/engbers-projektzentrale-updates/main/stable/Engbers_Projektzentrale_UPDATE_1_8_35_TINY.zip?rev=1835b-hidrive-share-pid' -o /tmp/pz1836b/base.zip
          unzip -q /tmp/pz1836b/base.zip -d /tmp/pz1836b/unpacked
          python build/1836/patch_1836b.py /tmp/pz1836b/unpacked
      - name: Verify
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('/tmp/pz1836b/unpacked/share_basket_v1821.py').read_text(encoding='utf-8')
          for marker in ('last_share_link','_current_share_link','HiDrive-Freigabelink wurde automatisch','my.hidrive.com/share/'):
              assert marker in p, marker
          print('OK')
          PY
      - name: Build package and manifest
        run: |
          python - <<'PY'
          import hashlib,json,pathlib,zipfile
          root=pathlib.Path('.'); src=pathlib.Path('/tmp/pz1836b/unpacked')
          zp=root/'stable/Engbers_Projektzentrale_UPDATE_1_8_36_TINY.zip'
          if zp.exists(): zp.unlink()
          with zipfile.ZipFile(zp,'w',zipfile.ZIP_DEFLATED) as z:
              for p in sorted(src.rglob('*')):
                  if p.is_file() and '__pycache__' not in p.parts and p.suffix!='.pyc':
                      z.write(p,p.relative_to(src).as_posix())
          with zipfile.ZipFile(zp) as z: assert z.testzip() is None
          sha=hashlib.sha256(zp.read_bytes()).hexdigest()
          m={
            'app':'Engbers Projektzentrale','channel':'stable','version':'1.8.36','published_at':'2026-09-18',
            'min_bootstrap_version':'1.8.3',
            'package_url':'https://raw.githubusercontent.com/Norbert1206/engbers-projektzentrale-updates/main/stable/Engbers_Projektzentrale_UPDATE_1_8_36_TINY.zip?rev=1836b-outlook-hidrive-link',
            'package_format':'zip','sha256':sha,
            'notes':'Version 1.8.36: Der zuletzt erzeugte HiDrive-Freigabelink wird beim Öffnen von OUTLOOK-MAIL automatisch in den Begleittext übernommen. Zusätzlich erkennt die Projektzentrale einen HiDrive-Freigabelink in der Zwischenablage, falls der Freigabekorb inzwischen neu geöffnet wurde. Alle Funktionen aus 1.8.35 bleiben erhalten.',
            'update_available':True
          }
          (root/'stable/manifest.json').write_text(json.dumps(m,ensure_ascii=False,indent=2)+'\n',encoding='utf-8')
          print(sha)
          PY
      - name: Commit release
        run: |
          git config user.name 'engbers-update-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add stable/Engbers_Projektzentrale_UPDATE_1_8_36_TINY.zip stable/manifest.json
          git diff --cached --quiet && exit 0
          git commit -m 'Release Projektzentrale 1.8.36 Outlook HiDrive link'
          git pull --rebase origin main
          git push origin main
